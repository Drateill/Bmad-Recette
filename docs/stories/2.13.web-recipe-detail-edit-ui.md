# Story 2.13: Web Recipe Detail & Edit UI

## Status
Draft

## Story
**As a** user,
**I want** to view full recipe details and edit recipes from the web app,
**so that** I can see all information and make changes easily.

## Acceptance Criteria

1. Recipe Detail page at /recipes/:id displays: large photo, title, description, prep/cook/total time, servings, rating
2. Ingredients section lists each ingredient with quantity, unit, name (with checkboxes for cooking mode)
3. Steps section displays numbered instructions with optional duration per step
4. Tags displayed as colored badges grouped by category
5. Action buttons: Edit Recipe, Delete Recipe, Adjust Portions (dropdown), Share (future)
6. Edit Recipe opens form in edit mode (inline or modal) with all fields populated
7. Form validation displays errors inline (required fields, format validation)
8. Portion adjustment shows real-time preview with slider/input (×0.5 to ×10 range)
9. Delete confirmation modal: "Are you sure? This cannot be undone."
10. Mobile-responsive layout: stacked on mobile, two-column on desktop

## Tasks / Subtasks

- [ ] Task 1: Create RecipeDetail page component (AC: 1, 10)
  - [ ] Create apps/web/src/pages/RecipeDetail.tsx
  - [ ] Route: /recipes/:id in React Router
  - [ ] Fetch recipe on mount: useRecipeDetail(id)
  - [ ] Display: Hero image, title, description, metadata (times, servings, rating)

- [ ] Task 2: Create ingredients section (AC: 2)
  - [ ] Component: RecipeIngredients
  - [ ] Display each ingredient: quantity, unit, name
  - [ ] Add checkboxes for cooking mode (local state, not persisted)
  - [ ] Strikethrough checked ingredients

- [ ] Task 3: Create steps section (AC: 3)
  - [ ] Component: RecipeSteps
  - [ ] Display numbered steps with instructions
  - [ ] Show duration if present
  - [ ] Cooking mode: highlight current step

- [ ] Task 4: Display tags (AC: 4)
  - [ ] Component: TagBadges
  - [ ] Group by category
  - [ ] Color-coded badges

- [ ] Task 5: Create action buttons (AC: 5)
  - [ ] "Edit Recipe" → navigate to edit mode or open modal
  - [ ] "Delete Recipe" → show confirmation modal
  - [ ] "Adjust Portions" → open portion slider modal

- [ ] Task 6: Implement edit mode (AC: 6, 7)
  - [ ] RecipeForm component (reusable for create/edit)
  - [ ] Pre-populate with recipe data
  - [ ] Form validation: Formik or React Hook Form
  - [ ] Submit: PUT /api/recipes/:id

- [ ] Task 7: Implement portion adjustment (AC: 8)
  - [ ] PortionAdjustmentModal component
  - [ ] Slider: 0.25x to 10x
  - [ ] Preview adjusted quantities in real-time
  - [ ] Apply button: Updates recipe view (not saved)

- [ ] Task 8: Implement delete confirmation (AC: 9)
  - [ ] DeleteConfirmationModal component
  - [ ] "Are you sure?" message
  - [ ] Confirm button: DELETE /api/recipes/:id
  - [ ] Redirect to /recipes on success

- [ ] Task 9: Add responsive layout (AC: 10)
  - [ ] Mobile: Stacked layout, full-width photo
  - [ ] Desktop: Two-column (photo left, details right)

- [ ] Task 10: Write tests
  - [ ] Test recipe detail renders
  - [ ] Test edit opens form
  - [ ] Test delete shows confirmation
  - [ ] Test portion adjustment

## Dev Notes

### Dependencies
- Story 2.5 (Get Recipe Details API) - MUST be complete
- Story 2.6 (Update Recipe API) - For editing
- Story 2.7 (Delete Recipe API) - For deletion
- Story 2.10 (Portion Adjustment) - For portion adjustment

### Component Structure
```
apps/web/src/
├── pages/
│   └── RecipeDetail.tsx
├── components/
│   ├── recipes/
│   │   ├── RecipeHero.tsx
│   │   ├── RecipeIngredients.tsx
│   │   ├── RecipeSteps.tsx
│   │   ├── RecipeForm.tsx (reusable)
│   │   ├── PortionAdjustmentModal.tsx
│   │   └── DeleteConfirmationModal.tsx
│   └── shared/
│       ├── TagBadges.tsx
│       └── StarRating.tsx
└── hooks/
    └── useRecipeDetail.ts
```

### API Integration
```typescript
const { data: recipe, isLoading } = useRecipeDetail(recipeId);

async function updateRecipe(data) {
  await apiClient.put(`/api/recipes/${recipeId}`, data);
}

async function deleteRecipe() {
  await apiClient.delete(`/api/recipes/${recipeId}`);
  navigate('/recipes');
}
```

### Testing

**Test File Location**: `apps/web/src/pages/__tests__/RecipeDetail.spec.tsx`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2026-01-10 | 0.1 | Initial story draft created from Epic 2 | Bob (Scrum Master Agent) |

## Dev Agent Record

*This section will be populated by the Dev Agent during implementation.*

## QA Results

*This section will be populated by the QA Agent after implementation review.*
